---

ssh_public_keys: "{{ vault_ssh_public_keys }}"
galaxy_team_users: "{{ vault_galaxy_team_users }}"
galaxy_cloud_team_users: "{{ vault_galaxy_cloud_team_users }}"
galaxy_admin_users: "{{ vault_galaxy_admin_users }}"
