---

host_users:
  - name: kxk302
    groups: sudo

host_authorized_key_users:
  - name: kxk302
    authorized:
      - kaivan_kamali

filesystems:
  - dev: /dev/vdb
    fstype: ext4

mounts:
  - name: /data
    src: /dev/vdb
    fstype: ext4
    mode: "0755"
    owner: kxk302
    group: kxk302
